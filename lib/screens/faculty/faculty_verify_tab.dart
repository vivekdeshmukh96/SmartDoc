import 'package:flutter/material.dart';

class FacultyVerifyTab extends StatelessWidget {
  const FacultyVerifyTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Verify Tab'));
  }
}
